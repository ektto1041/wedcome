# AGENTS.md

## 프로젝트 개요

이 프로젝트는 **모바일 청첩장 웹앱**을 만드는 것을 목표로 합니다.
주요 사용자는 모바일 기기에서 초대장을 열람하는 하객이며, 첫 화면 진입부터 주요 정보 확인까지의 흐름이 빠르고 자연스러워야 합니다.

기본 기술 스택:

- React
- Vite
- TypeScript
- Biome

## 기본 원칙

모든 작업은 아래 원칙을 우선합니다.

1. 모바일 우선
2. 빠른 첫 로딩
3. 읽기 쉬운 정보 구조
4. 과한 복잡성보다 단순하고 유지보수 가능한 구현
5. 접근성과 실제 사용성 고려

## 작업 방식

- 작은 단위로 변경하고, 변경 이유가 분명해야 합니다.
- UI를 만들 때는 모바일 화면을 기준으로 먼저 설계하고, 이후 태블릿/데스크톱 대응을 확장합니다.
- 새로운 추상화는 반복이 2~3회 이상 확인되기 전까지 서두르지 않습니다.
- 외부 라이브러리는 꼭 필요한 경우에만 추가합니다.
- 시각 효과보다 콘텐츠 전달을 우선합니다.

## 권장 초기 셋업

에이전트는 프로젝트 부트스트랩이 필요할 경우 아래 방향을 기본값으로 사용합니다.

- Vite의 React + TypeScript 템플릿 사용
- 패키지 매니저는 레포에 기존 흔적이 없다면 `pnpm` 우선
- 포맷팅/린팅은 Biome로 일원화
- 환경 변수는 초기에 최소화하고, 필요 시 `.env.example`을 함께 관리

예상 명령:

```bash
pnpm create vite . --template react-ts
pnpm add -D @biomejs/biome
pnpm biome init
```

명령은 실제 워크스페이스 상태와 충돌하지 않는지 확인 후 실행합니다.

## 폴더 구조 가이드

초기에는 과도한 구조 분리를 피하고, 아래 정도를 기본으로 삼습니다.

```text
src/
  assets/
  components/
  sections/
  hooks/
  utils/
  styles/
  App.tsx
  main.tsx
```

가이드:

- `components/`: 재사용 가능한 UI 조각
- `sections/`: 청첩장의 화면 단위 섹션
- `utils/`: 포맷팅, 날짜 처리 등 순수 함수
- `hooks/`: UI 상태/이벤트 관련 커스텀 훅
- `styles/`: 전역 스타일, 토큰, 테마 관련 파일

## UI 구현 원칙

- 모바일 기준 너비에서 가장 먼저 확인합니다.
- 첫 화면에는 핵심 정보만 배치합니다.
- 폰트, 여백, 대비는 가독성을 우선합니다.
- 스크롤 흐름은 자연스럽고 단절이 없어야 합니다.
- 애니메이션은 절제해서 사용하고, 정보 전달을 방해하지 않아야 합니다.

모바일 청첩장에서 우선 고려할 대표 섹션:

- 메인 비주얼
- 예식 일시
- 예식 장소
- 연락/공유
- 갤러리
- 마음 전하실 곳
- 오시는 길

## 접근성 원칙

- 시맨틱 HTML을 우선 사용합니다.
- 버튼과 링크의 역할을 혼동하지 않습니다.
- 이미지에는 적절한 대체 텍스트를 제공합니다.
- 색상만으로 의미를 전달하지 않습니다.
- 충분한 터치 영역을 확보합니다.

## 성능 원칙

- 불필요한 대형 의존성 추가를 피합니다.
- 이미지 최적화를 기본 전제로 둡니다.
- 초기 렌더에 필요 없는 리소스는 지연 로드합니다.
- 상태는 가능한 한 컴포넌트 가까이에 둡니다.
- 조기 최적화보다 단순한 구조를 유지하되, 모바일 성능 저하는 빠르게 수정합니다.

## TypeScript 원칙

- `any`는 특별한 이유가 없으면 사용하지 않습니다.
- 타입은 사용 위치와 가까운 곳에 둡니다.
- 컴포넌트 props 타입은 명확하게 선언합니다.
- 런타임 검증이 필요한 입력값은 타입만 믿지 말고 방어적으로 처리합니다.

## React 원칙

- 함수형 컴포넌트를 사용합니다.
- 컴포넌트는 하나의 명확한 역할을 갖도록 유지합니다.
- props drilling이 심해지기 전까지 전역 상태 도입을 서두르지 않습니다.
- 파생 가능한 값은 중복 상태로 저장하지 않습니다.
- 폼, 모달, 갤러리 같은 상호작용 요소는 키보드/터치 사용성을 함께 고려합니다.

## 스타일링 원칙

스타일링 방식은 초기 셋업 시점에 결정하되, 결정 전까지는 아래 기준을 따릅니다.

- 전역 토큰(색상, 간격, radius, 그림자)은 한 곳에서 관리
- 컴포넌트 내부 스타일 책임 범위는 명확하게 유지
- 하드코딩된 값의 반복을 줄이고 의미 있는 토큰 이름 사용
- 모바일에서 깨지는 고정 높이/너비 사용을 주의

스타일링 도구는 사용자가 명시하지 않았다면 추후 결정합니다.
후보는 CSS Modules, scoped CSS, 혹은 팀이 선호하는 경량 방식입니다.

### 레이아웃 폭 기준

모바일 청첩장은 화면 전체를 무조건 채우기보다, 읽기 좋은 콘텐츠 폭을 유지하는 방향을 기본값으로 삼습니다.

- viewport 자체에는 고정 폭을 강제하지 않습니다.
- 페이지 바깥쪽은 유동적으로 늘어나되, 실제 콘텐츠 영역은 `max-width`로 제한합니다.
- `max-width`를 넘는 큰 화면에서는 콘텐츠 폭은 고정되고 좌우 여백만 자연스럽게 늘어나는 구조를 사용합니다.
- 아주 작은 기기까지 고려해 콘텐츠 최소 기준은 `320px` 폭에서 깨지지 않도록 설계합니다.
- 강한 `min-width` 적용으로 가로 스크롤을 만드는 방식보다, `width: 100%`와 좌우 `padding`으로 안전하게 대응합니다.

권장 수치:

- 모바일 설계 기준 폭: `360px` 또는 `375px`
- 페이지 좌우 여백: 모바일 `16px`, 큰 화면 `24px~32px`
- 기본 콘텐츠 최대 폭: `720px`
- 텍스트 중심 좁은 본문 폭: `560px~640px`
- 갤러리, 지도, 이미지 중심 섹션 최대 폭: `720px`

권장 레이아웃 구조:

- `page`: 배경, 전체 세로 흐름, 바깥 여백 담당
- `container`: 전체 콘텐츠의 최대 폭 제한과 중앙 정렬 담당
- `content-narrow`: 문장 위주 섹션의 가독성 폭 제한 담당

예시 구조:

```css
.page {
  min-height: 100dvh;
  padding-inline: 16px;
}

.container {
  width: 100%;
  max-width: 720px;
  margin-inline: auto;
}

.content-narrow {
  width: 100%;
  max-width: 600px;
  margin-inline: auto;
}
```

섹션별 적용 기준:

- 메인 비주얼, 갤러리, 지도: `container` 폭 사용
- 인사말, 예식 정보, 교통 안내 텍스트: `content-narrow` 우선 사용
- 버튼 그룹, 연락처, 계좌 정보 카드: 모바일에서는 1열 기준으로 시작하고 폭 확장은 여백 증가보다 정보 밀도 증가가 필요한 경우에만 허용합니다.

## Biome 규칙

- 포맷팅과 린팅은 Biome 기준을 따릅니다.
- 별도 포매터/린터를 중복 도입하지 않습니다.
- 코드 스타일 논쟁보다 일관성을 우선합니다.

기본 실행 명령 예시:

```bash
pnpm biome check .
pnpm biome format --write .
```

## 테스트 원칙

초기 단계에서는 모든 것을 테스트하려 하기보다, 아래 우선순위를 따릅니다.

- 핵심 데이터 표시 로직
- 날짜/문구 포맷팅 유틸
- 상호작용이 있는 주요 UI

테스트 도구는 도입 시점에 결정하되, Vite 환경과 잘 맞는 구성을 우선합니다.

## 콘텐츠 관리 원칙

청첩장 데이터는 초기에 하드코딩해도 되지만, 구조는 분리 가능한 형태를 지향합니다.

예:

- 예식 정보 객체
- 연락처 목록
- 갤러리 이미지 배열
- 계좌 정보 목록

문구가 많아질수록 컴포넌트 내부에 직접 박아두기보다 별도 데이터 파일로 분리합니다.

## Git/변경 관리

- 한 변경은 하나의 목적을 갖도록 유지합니다.
- 의미 없는 대규모 포맷 변경은 피합니다.
- 사용자가 만든 기존 변경은 함부로 되돌리지 않습니다.
- 구조 변경 시에는 이유를 설명 가능한 수준으로 단순하게 유지합니다.

## 에이전트를 위한 실행 지침

- 작업 전 현재 파일 구조와 설정 상태를 먼저 확인합니다.
- 이미 존재하는 패턴이 있다면 새 패턴을 만들기보다 기존 패턴을 따릅니다.
- 사용자가 명시하지 않은 라이브러리는 자동 도입하지 않습니다.
- UI를 추가할 때는 모바일 화면 기준의 완성도를 먼저 확보합니다.
- 설정 파일을 추가할 때는 최소 구성으로 시작합니다.
- 문서와 코드가 어긋나면 코드 기준으로 확인 후 문서를 갱신합니다.
- 개발 서버가 실행 중인 상태에서 UI/CSS 코드를 수정하면 HMR로 바로 반영되므로, 브라우저 시각 검사는 기본적으로 사용자가 직접 확인합니다.
- 에이전트는 사용자가 명시적으로 요청한 경우나 빌드/타입/린트 오류 확인이 필요한 경우에만 별도 검사를 실행합니다.

## 당장 다음 단계 제안

초기 개발을 시작할 때는 아래 순서를 권장합니다.

1. Vite + React + TypeScript 프로젝트 생성
2. Biome 설정
3. 기본 폴더 구조 정리
4. 글로벌 스타일/디자인 토큰 설정
5. 메인 청첩장 섹션 골격 구현
6. 샘플 데이터 연결
7. 모바일 화면 기준 디테일 조정
